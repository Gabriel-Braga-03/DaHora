unit Model.Turma;

interface
    uses Model.Entidade;

    type
        TDiaS = (Segunda, Terça, Quarta, Quita, Sexta);
        TTurma = class (TEntidade)
            private
                Fserie: Byte;
                Fcomplemento: String;
            public
                property serie: Byte read Fserie write Fserie;
                property complemento: String read Fcomplemento write Fcomplemento;
        end;

implementation

end.
